statusCode : 200
delay: 0
headers:
  - 'Content-type:application/ld+json'
body: >

  {
    "@context": "https://solid.github.io/data-interoperability-panel/specification/interop.jsonld",
    "hasAccessConsents" : ["${SERVER_BASE}/access/all-1", "${SERVER_BASE}/access/registry-1", "${SERVER_BASE}/access/agent-1", "${SERVER_BASE}/access/selected-1"]
  }